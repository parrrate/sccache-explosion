[package]
name = "test-crate"
version = "0.0.0"
description = "test to check if sccache explodes"
edition = "2024"
publish = false

[dependencies]
cargo = { version = "0.91", features = ["all-static"] }
